// lexer should skip this &???//%#@

class Foo[T, V] extends List[List[T]] is
    var t: T
    var v: V
    this() is super() end
    Void Reverse() is
        var i: Integer := 0
        while (i.LessEq(this.Size.Divide(2))) loop
            this.Swap(i, this.Size.Minus(i.Plus(1)))
        end
    end
end

class Program is
    this() is
        var list: Foo[String, Real] := Foo[String, Real]()
        list.Add(List[String]())
        list.Add(List[String]())
        list.Reverse()
    end
end

// lexer should skip this &???//%#@
